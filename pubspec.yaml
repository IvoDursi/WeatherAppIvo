name: weather_app_ivo
description: A Very Good Project created by Very Good CLI.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  bloc: ^9.0.0
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.1
  freezed_annotation: ^2.4.1
  dartz: ^0.10.1
  google_places_flutter: ^2.1.0
  connectivity_plus: ^6.1.4
  animate_do: ^4.2.0
  geolocator: ^11.0.0
  geocoding: ^3.0.0
  weather_repository:
    path: packages/weather_repository
  open_weather_service:
    path: packages/open_weather_service
  local_storage_service:
    path: packages/local_storage_service
  google_places_service:
    path: packages/google_places_service

dev_dependencies:
  bloc_test: ^10.0.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^6.0.0
  freezed: ^2.4.5
  build_runner: ^2.4.6


flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/
